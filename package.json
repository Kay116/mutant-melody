{
  "name": "disease-in-d-minor",
  "version": "1.0.0",
  "description": "Educational demo that maps protein mutations to musical notes. Not medical advice.",
  "type": "commonjs",
  "scripts": {
    "lint": "node scripts/check-syntax.js",
    "test": "node --test",
    "check": "npm run lint && npm test",
    "start": "node server.js",
    "serve": "python -m http.server 8080"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  },
  "license": "SEE LICENSE — not yet chosen",
  "private": true
}
